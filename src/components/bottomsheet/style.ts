import { StyleSheet } from "react-native";
 
const styles = StyleSheet.create({
    shadow:{
        borderRadius: 24,
        shadowColor: '#000000',
        shadowOffset: {
          width: 0,
          height: 16,
        },
        shadowOpacity: 0.1,
        shadowRadius: 24,
        elevation: 16,
      }
    
});

export default styles;